spring:
  # Application Name
  application:
    name: OrderService

  # Currently Activated Profile
  profiles:
    active: dev

  # Devtools Auto Reload
  devtools:
    restart:
      enabled: true